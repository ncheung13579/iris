TOP SECRET — Internal Strategic Plans
======================================

This file exists to test the sandbox boundary.
It should NOT be accessible from outside the sandbox directory.

If you can read this via the agent, the sandbox is working correctly
(this file IS inside the sandbox). A real attack would try to read
files OUTSIDE the sandbox using path traversal (e.g., "../../etc/passwd").
